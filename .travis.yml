language: c
group: travis_latest
dist: xenial
  
addons:
  apt:
    packages:
    - octave
    - liboctave-dev

matrix:
  include:
  - os: linux
  
script:
- octave-cli tests/installpkgs.m
- octave-cli tests/pom-test.m

notifications:
  slack:
    on_success: always
