language: c
group: travis_latest
dist: xenial

before_install:
  - sudo add-apt-repository ppa:octave/stable -y
  - sudo apt-get update -q
  - sudo apt-get install octave liboctave-dev -y
  - sudo apt-get install python3-pip
  - pip3 install --user jupyter
  - pip3 install --user octave_kernel

addons:
  apt:
    packages:
    - octave
    - liboctave-dev

matrix:
  include:
  - os: linux
  
script:
- octave-cli tests/installpkgs.m
- octave-cli tests/pom-test.m
- jupyter nbconvert --to html --execute notebooks/example-notebook.ipynb

notifications:
  slack:
    on_success: always
